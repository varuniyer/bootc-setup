# local socket: trust (only reachable on-host)
local   all             all                                 trust

# tcp: experiments role into experiments db only, from localhost
host    experiments     experiments     127.0.0.1/32        trust
host    experiments     experiments     ::1/128             trust

# deny everything else
host    all             all             all                 reject
