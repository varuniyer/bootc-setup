# ----------------------------------------
# Local socket (only reachable inside container): trust everything
# ----------------------------------------
local   all             all                                 trust

# ----------------------------------------
# TCP: experiments DB only, from the container network
# ----------------------------------------
host    experiments     all             all                 trust

# ----------------------------------------
# deny everything else
# ----------------------------------------
host    all             all             all                 reject
