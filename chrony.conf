# Pinned to GCP's metadata-server time source so the firewall can scope
# NTP egress to 169.254.169.254 and skip DNS for the bootstrap query.
server 169.254.169.254 iburst

driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
